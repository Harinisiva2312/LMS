services:
  - type: web
    name: LMS-1
    runtime: static
    buildCommand: cd lms-frontend && npm install && npm run build
    staticPublishPath: lms-frontend/build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html